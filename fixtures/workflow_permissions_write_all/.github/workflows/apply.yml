name: Terraform Apply

on:
  push:
    branches: [main]

permissions: write-all

jobs:
  apply:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
      - uses: actions/checkout@v4
      - run: terraform init
      - run: terraform plan
