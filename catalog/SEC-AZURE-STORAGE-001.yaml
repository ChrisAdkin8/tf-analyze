id: SEC-AZURE-STORAGE-001
title: "Azure storage account allows non-HTTPS traffic"
section: security
default_urgency: HIGH
blast_radius: single-resource
status: stub
patterns:
  - kind: resource_arg
    resource: azurerm_storage_account
    arg: enable_https_traffic_only
    regex: '^false$'
recommendation: |
  TODO: stub. Detect `azurerm_storage_account` blocks where
  `enable_https_traffic_only` is set to false (or absent — the provider
  default flipped between major versions). Recommend setting it to true
  explicitly, plus `min_tls_version = "TLS1_2"`.
verification: |
  TODO
fixtures: []
