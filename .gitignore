__pycache__/
*.pyc
.DS_Store
docs/sentinel/
/tmp/
*.sarif
*.html
!reports/*.html
!demo/*.html
# Google Search Console verification files. Required to live at the
# site root for ownership verification — must be tracked.
!docs/google*.html
# Local-only working strategy docs — kept in the repo workspace for
# easy editing but not version-controlled. Excluded by explicit paths
# (not a glob) so unrelated files in docs/launch/ stay tracked.
docs/launch/virality-plan.md
docs/launch/scan-service-plan.md
docs/launch/scan-service-todo.md
